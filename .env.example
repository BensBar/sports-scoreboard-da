# Example environment configuration (placeholders; not all used yet)
# Copy to .env and adjust as needed.

VITE_API_BASE_URL=https://example.api.provider
VITE_WEBSOCKET_URL=wss://example.api.provider/live
VITE_DEFAULT_LEAGUE=NFL
